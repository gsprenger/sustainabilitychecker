<!-- HEADER -->
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#header">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#home"><%= Content.text('st_header_title') %></a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="header">
      <ul class="nav navbar-nav">
        <li class="<%= if @isHome then 'active' else '' end %>">
          <a href="<%= if @isHome then '' else '/home' end %>#home"><%= Content.text('st_header_home') %></a>
        </li>
        <li>
          <a href="<%= if @isHome then '' else '/home' end %>#more"><%= Content.text('st_header_more') %></a>
        </li>
        <li>
          <a href="<%= if @isHome then '' else '/home' end %>#disclaimer"><%= Content.text('st_header_disclaimer') %></a>
        </li>
        <li>
          <a href="<%= if @isHome then '' else '/home' end %>#about"><%= Content.text('st_header_about') %></a>
        </li>
        <li>
          <a href="<%= if @isHome then '' else '/home' end %>#contact"><%= Content.text('st_header_contact') %></a>
        </li>
        <li>
          <a href="<%= if @isHome then '' else '/home' end %>#credits"><%= Content.text('st_header_credits') %></a>
        </li>
      </ul>
    </div>
  </div>
</nav>
